# Dev Container

This repo is configured to run in GitHub Codespaces / VS Code Dev Containers.

## Included

- Latest Python 3.x base devcontainer image
- PowerShell (`pwsh`) installed and set as the default integrated terminal
- Python packages installed on create: `python-dotenv`, `openai`
- VS Code extensions suggested/installed: GitHub Copilot + Copilot Chat
